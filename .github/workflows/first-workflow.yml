name: My first workflow

on: push

jobs:
    my-first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v5
            - name: Welcome, list and read the files
              run: |
                   echo "Hello, world!"
                   ls -la
                   cat README.md